language: java
sudo: false

jdk:
  - oraclejdk8

sudo: required

before_install:
 - chmod +x gradlew

after_success:
  - bash <(curl -s https://codecov.io/bash) -t token <(curl -s https://codecov.io/bash)